# SQL Data Integrity Checks ✅

A collection of **SQL scripts** to validate and monitor **data quality & consistency** across relational databases.  
Supports **MySQL** and **PostgreSQL** (with minor syntax differences).

## Features
- 🔍 Detect NULL values in critical columns  
- 🔁 Identify duplicates in unique fields  
- 🔗 Check referential integrity across related tables  
- 🧪 Includes a sample schema for testing  

## Usage
1. Clone the repo:
   ```bash
   git clone https://github.com/marierabill/sql-data-integrity-checks.git
   cd sql-data-integrity-checks
